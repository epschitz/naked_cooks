<h2><%= @recipe.name %></h2>
<%= link_to(image_tag(@recipe.image.url(:thumb)), @recipe.image.url(:big)) %>
<%= raw @recipe.description %>
<%= link_to 'Edit', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>
